[package]
name = "mallows_vs_dango"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Optimize deps in dev for playable framerates
[profile.dev.package."*"]
opt-level = 2
